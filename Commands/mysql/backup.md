# MySQL Backup Command

Run this exactly. Do not interpret, modify, or handle errors yourself. Just run it and show the output.

```bash
source ~/.claude/mysql.env && python3 ~/.claude/scripts/mysql-backup.py
```
