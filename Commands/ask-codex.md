# Ask Codex (Deprecated)

**This skill is deprecated.** Use `/codex` instead — it handles long prompts, special characters, and runs autonomously.

For critical reviews of plans or files, use `/codex-review`.

## Legacy Usage

`/ask-codex <prompt>`

## Instructions

When this command is invoked, redirect to `/codex` with the same arguments. Use the Skill tool to invoke `codex` with the provided prompt.
